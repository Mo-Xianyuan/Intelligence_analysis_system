#!/usr/bin/env python
#encoding=utf-8

from create_inversion import Create_Inversion

if name = '__main__':
    inversion = Create_Inversion()
    word = raw_input('输入关键词')
    if not inversion[word]:
        print('not found')
    else:
        print inversion[word]
